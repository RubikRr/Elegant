@model List<Elegant.Web.ViewModels.ProductViewModel>
@{
    ViewData["Title"] = "Home Page";
}

@await Html.PartialAsync("SearchProducts") 
@if(Model==null || Model.Count==0)
{
    <h1>На данный момент каталог пуст</h1>
}
@await Html.PartialAsync("HomeProducts")



